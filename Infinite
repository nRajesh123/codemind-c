/*Write a Program that takes a number as input and prints its square infinitely many times until -I is given as input.


#include<stdio.h>
int main(){
    int n;
    while(1){
        scanf("%d",&n);
        if(n==-1)break;
        printf("%d\n",n*n);
    }
}
